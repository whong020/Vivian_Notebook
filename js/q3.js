var sumLists = function(list1, list2) {
    return 1;
};


/**
 * DESIGN
 * A function which will return the sum of two numbers stored in a list
 * to the target
 * 
 * Parameters: obj , obj
 * List will be an object the following:
 *  { val: 3, next: {val: 2, next: null}} would be the number 32 
 * The test spec is called with something like sumList(list1,list2 )
 * Returns: int
 */